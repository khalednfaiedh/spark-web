import { NameComptePipe } from './name-compte.pipe';

describe('NameComptePipe', () => {
  it('create an instance', () => {
    const pipe = new NameComptePipe();
    expect(pipe).toBeTruthy();
  });
});
